Python Stuff
========================================================
**A bunch of python stuff**

# Basic commands

Build:
```shell
docker compose build
```

Run:
```shell
docker compose up
```

Bash into:
```shell
docker compose run --rm -it app bash 
```
